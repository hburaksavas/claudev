/**
 * Pure-Java domain aggregates and enums for the workspace/instance/pipeline/operation model.
 * No I/O, no Spring annotations, no dependency on {@code provider-api}'s wire DTOs (see D2 in
 * docs/adr — domain types are explicitly not assumed to equal future wire DTOs).
 */
package dev.claudev.domain;
